<?xml version="1.0" encoding="UTF-8"?>
<component name="IntroVideoScreen" extends="Group" initialFocus="videoPlayer">

    <interface>
        <!-- Signal sent to MainScene when splash finishes -->
        <field id="done" type="boolean" alwaysNotify="true" />
    </interface>

    <children>

        <!-- Black background -->
        <Rectangle
            id="bg"
            width="1280"
            height="720"
            color="0x000000FF" />

        <!-- Splash video -->
        <Video
            id="videoPlayer"
            width="1280"
            height="720"
            translation="[0,0]"
            loop="false"
            control="stop"
            visible="true" />

    </children>

    <script type="text/brightscript" uri="pkg:/components/IntroVideoScreen.brs" />

</component>
